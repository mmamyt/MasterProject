package polymorphism;

public class Theory {
    //Polymorphism mean "many forms" and it occurs when we have classes that are related to each other by inheritance
    //it is the ability of  an object to take on many forms. The most common use of polymorphism in OOP occurs when a parent class reference is used to refer to a child class object
    //it is important to know that the only possible
    //it increases reusablity

    //types:
    //run time (eg @Overriding)
    //compile time (eg static binding or method overloading) is also known as static polymorphism.


    //3 types of variables: local variable, instance variable,
}
